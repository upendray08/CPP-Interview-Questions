#include <bits/stdc++.h>
// #include <string.h>
using namespace std;
int main()
{
    string a;
    string b;
    cin>>a>>b;
    cout<<strcmp(a.c_str(),b.c_str())<<endl; // -1 if not same or if same 0 
    string d ;
    // strcpy(d.c_str(),a.c_str());
    cout<<d<<endl;

    return 0;
}