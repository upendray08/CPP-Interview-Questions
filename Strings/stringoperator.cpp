#include <bits/stdc++.h>
using namespace std;
int main()
{
    string str;
    getline(cin,str);
    // for(int i=0;i<str.size();i++){
    //     if(str[i]==' '){
    //         str.erase(i,1);
    //     }
    // }
    erase(str.begin()+i)
    // earse(index,count)
    cout<<a<<endl;
    // cout<<str<<endl;
    return 0;
}